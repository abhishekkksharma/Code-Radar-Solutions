#include <stdio.h>
int main(){
    int a,b;
    scanf("%d %d",&a,&b);
    int arr[n];
    int count=0;
    for(int i=0;i<n;i++){
        if(arr[i]==b){
            count++;
        }
    }
    printf("%d",count);
    return 0;
}